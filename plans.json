[
  {
    "plan": "PowerME Multi-Feature Implementation Plan (8 items):\n1. HealthConnect Body Measurements — Full HC SDK integration (READ_WEIGHT + READ_BODY_FAT) with runtime permission request, getLatestWeight()/getLatestBodyFat() in HealthConnectManager, syncFromHealthConnect() in SettingsViewModel, Sync button + HC source label + error display in SettingsScreen.\n2. Gemini Model List 24h Refresh — Added modelsLastFetched to AppSettingsDataStore, AppStartupViewModel triggers background model refresh if >24h since last fetch, SettingsViewModel.fetchModelsIfNeeded() also checks timestamp.\n3. War Room Clear Button Fix — clearChat() now resets generativeModel=null, sets LOADING state, and re-runs initializeSession() so Gemini has no memory of prior messages.\n4. Clocks Tab Overhaul — Renamed 'Tools' tab to 'Clocks'; reordered 2x2 grid (Stopwatch|Timer / Tabata|EMOM); friendly ModeCard labels; EMOM skip-last-rest toggle (silent stop); Countdown pre-finish alert field; 2s/1s warning beeps + 800ms end beep via new notifyEnd(); input validation (digits-only, allow empty clear).\n5. App Launcher Icon — Created ic_launcher_logo.xml bitmap wrapper for logo.jpeg; updated mipmap-anydpi adaptive icon XMLs to use it as foreground.\n6. Rest Timer Audio in Active Workout — Removed hasNotifiedAt5Seconds; now plays warning beeps at 2s/1s remaining and calls notifyEnd() at 0s.\n7. Superset Support — Room v15→v16 migration (supersetGroupId TEXT on workout_sets); ExerciseWithSets + ActiveWorkoutState extended; pairAsSuperset(), removeFromSuperset(), advanceSupersetTurn() in WorkoutViewModel; SUPERSET header chip, SS badge, border glow for active turn, MoreVert menu with Pair/Remove options, SupersetPartnerPickerDialog in ActiveWorkoutScreen.\n8. RestTimerNotifier.notifyEnd() — New method (800ms beep + vibration) shared by clocks finishTimer() and workout rest timer.",
    "timestamp": "2026-02-20T14:30:00Z"
  }
  ,{
    "plan": "PowerME v2.0 Surgical Refactor (8 grouped changes): 1. Color system — Slate950/900/800 dark palette, Slate50/100/200 light palette, TimerGreen updated to Emerald400 (#34D399), legacy aliases (OledBlack, DeepNavy, NavySurface, SlateGrey) remapped to Slate tokens; Theme.kt updated with surfaceVariant + error tokens. 2. Tab reorder — Workouts|Clocks|Exercises|History|Trends (PowerMeNavigation.kt). 3. Exercise Management Hub — ModalBottomSheet (8 items: session note, sticky note, warmup sets, rest timer, replace, superset, preferences, remove) replaces DropdownMenu in ExerciseCard; ManagementHubSheet + RestTimerSheet + ExerciseNoteDialog composables added (ActiveWorkoutScreen.kt). 4. WorkoutViewModel — ExerciseWithSets gains sessionNote+stickyNote fields; ActiveWorkoutState gains isSupersetSelectMode+supersetCandidateIds+snackbarMessage; new functions: removeExercise, replaceExercise, updateExerciseSessionNote, updateExerciseStickyNote (persisted), updateExerciseRestTimer, addWarmupSetsToExercise, enterSupersetSelectMode, toggleSupersetCandidate, commitSupersetSelection, exitSupersetSelectMode, reorderExercise, pairAsSuperset(Set<Long>), clearSnackbarMessage; RoutineExerciseCrossRefDao injected; sticky note loaded in addExercise(). 5. Superset multi-select CAB — Contextual Action Bar shown above exercise list when isSupersetSelectMode; checkboxes on ExerciseCard; Sync button commits selection. 6. DB migration v16→v17 — stickyNote TEXT added to routine_exercise_cross_ref; MIGRATION_16_17 registered in DatabaseModule.kt; DB_UPGRADE.md created at project root. 7. WorkoutTimerService — ForegroundService with notification channel, countdown coroutine, TimerBinder, StateFlow; registered in AndroidManifest with FOREGROUND_SERVICE + POST_NOTIFICATIONS permissions. 8. Build — sh.calvin.reorderable:reorderable:2.5.1 dependency added (artifact corrected from plan; reorderable-compose does not exist on Maven Central). Files: Color.kt, Theme.kt, PowerMeNavigation.kt, RoutineExerciseCrossRef.kt, PowerMeDatabase.kt, RoutineExerciseCrossRefDao.kt, DatabaseModule.kt, WorkoutViewModel.kt, ActiveWorkoutScreen.kt, WorkoutTimerService.kt (new), AndroidManifest.xml, app/build.gradle.kts, DB_UPGRADE.md (new), CLAUDE.md.",
    "timestamp": "2026-02-20T16:00:00Z"
  }
  ,{
    "plan": "PowerME v2.0 Batch Bug Fixes & Feature Expansion (10 sections + 4 directives): S1.1 War Room overlay — ChatViewModel.dismissAllOverlays() added; DisposableEffect in PowerMeNavigation WAR_ROOM composable; ChatViewModel hoisted; D4 popBackStack guard on tab onClick when on WAR_ROOM. S1.2 YouTube Intent — ExercisesScreen replaces YouTubePlayerBottomSheet with vnd.youtube: Intent + browser fallback, wrapped in try/catch. S2.1 Multi-select muscles — ExercisesUiState.selectedMuscles: Set<String>; onMuscleFilterToggled() toggles in/out; applyFilters() uses isEmpty()/in. S2.2 Equipment filter — selectedEquipment: Set<String>; onEquipmentFilterToggled(); applyFilters() AND-combined. D1 Filter visuals — separate Muscle/Equipment label rows, HorizontalDivider alpha=0.15, Equipment chips use ElectricBlue. S3.1 Dumbbell slider — horizontal Row with two weight(1f) columns. S3.2 Barbell toggle — ON sets hasBench=true+ALL_PLATES; OFF clears hasBench+plates; toggleBench() no longer touches plates; Bench conditionally in standardItems list; Plates Card wrapped in if(hasBarbell). S3.3 Model resolver — GymSetupViewModel injects ModelRouter; analyzeImage()+extractEquipmentFromText() use modelRouter.resolveEnrichmentModel(null); ChatViewModel injects AppSettingsDataStore; createGenerativeModel()/greeting/statePatch/deltaSummary use appSettingsDataStore.warRoomModel.first(). S3.4 Gym Inventory — savedProfileId: Long? in GymSetupUiState; saveGym() captures insertedId; GymSetupScreen LaunchedEffect(savedProfileId)+onSaved(Long) param; PowerMeNavigation GYM_INVENTORY route + composable; new GymInventoryScreen.kt with GymInventoryViewModel (loads profile by id), categorized sections (Standard/Plates/DumbbellRange/Additional). S4.1 HC fix — AndroidManifest READ_EXERCISE permission; HealthConnectManager hasPermission() helper + permission check in getLatestWeight()/getLatestBodyFat(); getExerciseSessions() method. D2 HC UX — checkPermissionsGranted() in HealthConnectManager; hcPermissionsMissing: Boolean in SettingsUiState; syncFromHealthConnect() checks permissions first and sets flag if missing. D3 Model safety — isModelsReady: Boolean in GymSetupUiState; GymSetupViewModel.init() triggers fetchModels() if key exists but models empty; Analyze+Extract buttons disabled when !isModelsReady; 'Loading models…' text shown. 4.2 ServerException — ChatViewModel.sendMessage() adds specific catch for ServerException before general Exception with quota/model-not-found error messages. Files: ChatViewModel.kt, PowerMeNavigation.kt, ExercisesViewModel.kt, ExercisesScreen.kt, GymSetupViewModel.kt, GymSetupScreen.kt, GymInventoryScreen.kt (new), HealthConnectManager.kt, SettingsViewModel.kt, AndroidManifest.xml.",
    "timestamp": "2026-02-20T18:30:00Z"
  }
  ,{
    "plan": "PowerME v2.0 Sprint — 3 tasks: (1) Health Connect Siphon — replaced null stubs in HealthConnectManager.kt with real SDK queries: getSleepDurationMinutes() reads SleepSessionRecord (maxByOrNull endTime → ChronoUnit.MINUTES), getHeartRateVariability() reads HeartRateVariabilityRmssdRecord (RMSSD ms), getRestingHeartRate() reads RestingHeartRateRecord (beatsPerMinute.toInt()), getSteps() sums StepsRecord from local midnight to now; added READ_SLEEP, READ_HEART_RATE_VARIABILITY, READ_RESTING_HEART_RATE, READ_STEPS permissions to AndroidManifest.xml. (2) Timer Service Binding — WorkoutViewModel now binds WorkoutTimerService in init via ServiceConnection (BIND_AUTO_CREATE); onServiceConnected launches serviceCollectionJob to mirror service.timerState (isRunning=true only) into _workoutState.restTimer; private onTimerTick() plays warning beeps at 2s/1s; private onTimerFinish() fires notifyEnd() + clears restTimer state; startRestTimer() delegates to service.startTimer(::onTimerTick, ::onTimerFinish) when bound, falls back to in-process coroutine; timerJob?.cancel() guard at top of startRestTimer() prevents double-beep race; addTimeToTimer() restarts service timer with extended duration when bound; skipRestTimer() calls service.stopTimer() when bound; onCleared() unbinds and cancels all jobs. (3) Theme Audit — added MedicalAmber (#FFC107) and MedicalAmberContainer (#FFA000) to Color.kt; replaced 6 hardcoded Color(0xFF...) literals in MedicalRestrictionWarning composable (ActiveWorkoutScreen.kt): RED-list container → MaterialTheme.colorScheme.error.copy(alpha=0.15f), RED-list text → MaterialTheme.colorScheme.error / .copy(alpha=0.85f), YELLOW-list container → MedicalAmberContainer.copy(alpha=0.12f), YELLOW-list text → MedicalAmber / .copy(alpha=0.9f); added MedicalAmber+MedicalAmberContainer imports to ActiveWorkoutScreen.kt. Files: HealthConnectManager.kt, AndroidManifest.xml, WorkoutViewModel.kt, Color.kt, ActiveWorkoutScreen.kt, CLAUDE.md.",
    "timestamp": "2026-02-21T00:00:00Z"
  }
  ,{
    "plan": "PowerME v2.0 Final Surgical Polish (3 areas, 6 files): (1) Exercise Filter Chips — EQUIPMENT_TYPES updated to [All, Barbell, Dumbbell, Machine, Cable, Bodyweight] (removed Bench/Smith Machine); equipment label+chip row wrapped in Surface(color=MaterialTheme.colorScheme.surfaceVariant) for visual distinction from muscle row; applyFilters() updated to case-insensitive comparison for both muscleGroup and equipmentType using String.equals(ignoreCase=true). (2) Bayesian M-Estimate — calculateBayesian1RM(sampleMean, sampleSize, priorMean, confidenceConstant=5) added to StatisticalEngine.kt after calculate1RM(); implements μ_bayesian=(C*μ_prior+n*μ_sample)/(C+n); guards for sampleSize<=0 (return priorMean) and priorMean==0.0 (return sampleMean). (3) Gym Module Polish — GymSetupViewModel: removed updateDumbbellMin/updateDumbbellMax, added updateDumbbellRange(ClosedFloatingPointRange<Float>); GymSetupScreen: two-Slider side-by-side layout replaced with single RangeSlider; GymInventoryScreen: FlowRow+SuggestionChip grids replace InventoryRow bullet lists for Standard Equipment, Available Plates, and Additional Equipment sections; InventoryRow composable removed; Slate800 import added; @OptIn(ExperimentalLayoutApi::class) added. Files: ExercisesScreen.kt, ExercisesViewModel.kt, StatisticalEngine.kt, GymSetupScreen.kt, GymSetupViewModel.kt, GymInventoryScreen.kt.",
    "timestamp": "2026-02-21T10:00:00Z"
  }
  ,{
    "plan": "PowerME 'Pure Performance' Phase (4 areas, 9 files): (1) Stremio Theme Overhaul — Color.kt: replaced Slate/Facebook-Blue palette with Stremio tokens (StremioBackground #0F0F1E, StremioSurface #191932, StremioSurfaceVar #1F1F3E, StremioViolet #7B5BE4, StremioMagenta #B3478C, StremioCloudGrey #E1E1E6, StremioError #FF4444); legacy aliases (DeepNavy, NavySurface, SlateGrey, OledBlack, NeonBlue, ElectricBlue, Slate200) remapped to nearest Stremio tokens for source compatibility; Theme.kt DarkColorScheme updated to use Stremio tokens. (2) AI De-coupling — PowerMeNavigation.kt: removed War Room Forum IconButton from TopAppBar; replaced DeepNavy/NavySurface/NeonBlue hardcodes with MaterialTheme.colorScheme.* tokens in scaffold (TopAppBar, NavigationBar, NavigationBarItem); GymSetupScreen.kt: removed 'Equipment Photo + Gemini Analysis' and 'Describe Your Gym + Extract Equipment' AI sections; replaced with manual equipment input (OutlinedTextField + Add IconButton + InputChip removable list); GymSetupViewModel.kt: added addEquipmentManually(item: String); all hardcoded colors in GymSetup replaced with MaterialTheme tokens; WeeklyInsightsAnalyzer: already correctly receives bayesian1RMs — AnalyticsRepository already passes it; no change required. (3) Bug Squashing — GymSetupViewModel.updateDumbbellRange(): added defensive coerceAtMost/coerceAtLeast guard to prevent min>max edge cases; ActiveWorkoutScreen LazyColumn: added contentPadding = PaddingValues(bottom = 8.dp) to ensure last set row remains visible above action buttons when keyboard is open; imePadding() already correct on outer Column; WorkoutTimerService: timerJob?.cancel() guard already prevents double-beep race; service + ServiceConnection binding already correct — no change required. (4) UI Polish — GymInventoryScreen.kt: all FlowRow SuggestionChip colors (3 sections) updated to MaterialTheme.colorScheme.surfaceVariant/primary/secondary; InventorySection card updated to MaterialTheme.colorScheme.surface; all hardcoded NeonBlue/NavySurface/DeepNavy/Slate800/ElectricBlue/Color.White replaced with theme tokens; unused color imports removed; ExercisesScreen.kt: added toEquipmentDisplayName() String extension to convert DB UPPER_CASE equipment type to Title Case; applied to exercise chip label. Files: Color.kt, Theme.kt, PowerMeNavigation.kt, GymSetupScreen.kt, GymSetupViewModel.kt, GymInventoryScreen.kt, ExercisesScreen.kt, ActiveWorkoutScreen.kt.",
    "timestamp": "2026-02-21T12:00:00Z"
  }
  ,{
    "plan": "PowerME v3.0 Reconciled Sprint (6 tasks, 17 files): (0) SurgicalValidator.kt — new utility (util/SurgicalValidator.kt) with parseDecimal() (locale-aware commas+periods), parseReps() (integer only), isLeakedMetric() runtime check, MIGRATION_SQL const val; no inline try-catch in ViewModels per ProjectMap §3. (1) Task 4 Navigation — tabs reordered to Exercises|History|Workouts|Clocks|Trends in PowerMeNavigation.kt line 286. (2) Task 0 Theme §1 compliance — removed all static color imports (NeonBlue, OledBlack, DeepNavy, NavySurface, SlateGrey, ElectricBlue) from 8 composable files; replaced with MaterialTheme.colorScheme.{primary,background,surface,onSurface,secondary} tokens; imports preserved in Color.kt. (3) Task 2 Gym Setup — removed if(hasBarbell) guard from Bench toggle (now always in standardItems); removed if(hasBarbell) outer guard from Plates card; Plates card gets .alpha(if(selectedPlates.isNotEmpty()) 1f else 0.45f) greyed-out disabled state; added alpha import to GymSetupScreen.kt. (4) Task 1 P0 Crash Fix — WorkoutViewModel: added SurgicalValidator import; new onWeightChanged(exerciseId, setOrder, raw) and onRepsChanged(exerciseId, setOrder, raw) functions with parseDecimal/parseReps + ghost fallback; finishWorkout() weight parsing replaced with SurgicalValidator.parseDecimal + ghostWeight fallback; SetRow gains optional onWeightChanged/onRepsChanged callbacks (defaults to onUpdateSet); ExerciseCard threads them; ActiveWorkoutScreen wires to ViewModel with rest-timer trigger preserved. (5) Task 5 Migration 17→18 — PowerMeDatabase version 17→18; MIGRATION_17_18 using SurgicalValidator.MIGRATION_SQL added to DatabaseModule.kt; SurgicalValidator.MIGRATION_SQL @Query added to ExerciseDao.clearLeakedMetricNotes(); DB_UPGRADE.md updated. (6) Task 3 Health Connect Height — AndroidManifest READ_HEIGHT permission; HealthConnectManager: HeightRecord import, getLatestHeight() (365-day window, inMeters×100→Float), checkPermissionsGranted() now includes HeightRecord; MetricType enum gains HEIGHT entry; SettingsViewModel: heightInput+lastHeight in SettingsUiState, UserSessionManager injected, loadUserHeight() in init, updateHeightInput() with SurgicalValidator validation, syncFromHealthConnect() fetches height, saveBodyMetrics() full dual-sink (MetricLog+User entity) for weight/bodyFat/height; SettingsScreen: READ_HEIGHT in HC launcher, lastText includes height, Height OutlinedTextField below weight/bodyFat row.",
    "timestamp": "2026-02-21T20:00:00Z"
  }
  ,{
    "plan": "PowerME v3.0 Stability & Cleanup Sprint (5 phases, 13 files): Phase 1 — P0 Crash Fix: WorkoutViewModel.onWeightChanged/onRepsChanged now early-return on Invalid (no state mutation); removed isCompleted side-effect from both handlers; new completeSet(exerciseId, setOrder) is the sole setter of isCompleted=true, gated behind dual SurgicalValidator parseDecimal+parseReps checks, starts rest timer only after confirmed completion; eliminates timer race condition triggered on every keystroke. Phase 2 — DB Migration v18→v19: SurgicalValidator.MIGRATION_SQL_V19 const added (strips '181.5 cm:' prefix via REPLACE+NULLIF); MIGRATION_18_19 object added to DatabaseModule.kt and registered in addMigrations(); PowerMeDatabase bumped to version 19; DB_UPGRADE.md updated with v19 entry. Phase 3 — Local Exercise Search: ExerciseDao.searchExercises(query) @Query added (case-insensitive LIKE, prefix-priority ORDER BY, LIMIT 25); ExerciseRepository.searchExercises() exposed; MagicAddViewModel gains _searchResults StateFlow + onSearchChanged(query) that calls repository; reset() also clears searchResults; MagicAddDialog rewritten — search field triggers onSearchChanged on every keystroke; local results appear as clickable LazyColumn rows (max 240dp height); 'Create new' row at bottom triggers existing Gemini enrichment when results < 25; Gemini SEARCH button removed from main flow; ADD button only shown in Found state. Phase 4 — ExercisesScreen UI Cleanup: VideoLibrary play icon removed from ExerciseCard; ExerciseCard gains onClick parameter; selectedExercise: MutableState<Exercise?> at screen scope; card tap opens ExerciseDetailSheet (ModalBottomSheet) with name, muscle/equipment SuggestionChips, setupNotes in muted-gold Surface (Color 0xFF5A4D1A), YouTube TextButton via vnd.youtube: intent + browser fallback; all hardcoded NeonBlue/ElectricBlue/NavySurface replaced with MaterialTheme.colorScheme.primary/secondary/surface tokens. Phase 5 — Clocks Persistent Buttons: ToolsScreen button row wrapped in if(TABATA||EMOM)/else; TABATA+EMOM get persistent weight(1f) Start+Reset buttons always visible regardless of phase; STOPWATCH+COUNTDOWN keep existing icon-labeled toggle+reset layout.",
    "timestamp": "2026-02-22T00:00:00Z"
  }
  ,{
    "plan": "PowerME v3.0 'Strong Aesthetic' Sprint (12 steps, 12 files): Step 1 — Color.kt: added StremioInputPill = Color(0xFF2C2C4E) after StremioSurfaceVar. Step 2 — Created RoutineExercise.kt entity (data/database) with Room @Entity, foreign keys to routines+exercises, indices on routineId+exerciseId, fields: id, routineId, exerciseId, sets, reps, restTime, order (backtick-escaped in queries), supersetGroupId. Step 3 — Created RoutineExerciseDao.kt: getForRoutine (ORDER BY `order`), insert, insertAll, delete, deleteByRoutineAndExercise, updateOrder. Step 4 — Routine.kt: added isArchived: Boolean = false field (v20 soft-archive). Step 5 — PowerMeDatabase.kt: bumped version 19→20, added RoutineExercise::class to entities, added abstract routineExerciseDao(); DatabaseModule.kt: added MIGRATION_19_20 (ALTER TABLE routines ADD COLUMN isArchived, CREATE TABLE routine_exercises with FK+indices), appended to addMigrations(), added provideRoutineExerciseDao() Hilt provider. Step 6 — Created WorkoutInputField.kt (ui/components): BasicTextField-only, StremioInputPill background, RoundedCornerShape(4.dp), 34dp height, 1dp primary border on focus via collectIsFocusedAsState(). Step 7 — ActiveWorkoutScreen.kt: added WorkoutSetRow composable (Set# 24dp | Previous 72dp | KG pill weight(1f) | REPS pill weight(1f) | ✓ button 36dp with TimerGreen/grey state); added onCompleteSet: (Int)->Unit parameter to ExerciseCard; replaced SetRow() call in else branch with WorkoutSetRow(); wired onCompleteSet -> viewModel.completeSet() at ExerciseCard invocation site; added imports for WorkoutInputField, TimerGreen, RoundedCornerShape, TextAlign. Step 8 — ActiveWorkoutScreen.kt: restructured ExerciseCard Card content from Card{Column{}} to Card{Row(IntrinsicSize.Min){Box(4dp spine, fillMaxHeight, secondary color if isInSuperset) + Column(weight(1f))}}; original Column content unchanged. Step 9 — RoutineDao.kt: added RoutineExerciseNameRow data class; added getAllActiveRoutinesWithExerciseNames() and getAllArchivedRoutinesWithExerciseNames() Flow queries with LEFT JOIN routine_exercises+exercises. Step 10 — WorkoutsViewModel.kt: full overhaul — RoutineWithSummary(routine, exerciseNames, daysSincePerformed?) data class; activeRoutines+archivedRoutines StateFlows from DAO queries collapsed via groupBy; archiveRoutine, unarchiveRoutine, deleteRoutine, renameRoutine suspend funs. Step 11 — WorkoutsScreen.kt: full overhaul — removed all NeonBlue/NavySurface/OledBlack static imports; all colors use MaterialTheme.colorScheme tokens; 'Start Empty Workout' full-width Button at top; RoutineCard shows name (bold 17sp), exercise summary (take(3)+ellipsis), recency label; 3-dot DropdownMenu with 5 items (Edit, Rename, Archive/Unarchive, Duplicate, Delete); archived section with AnimatedVisibility toggle; rename AlertDialog with OutlinedTextField. Step 12 — WorkoutRepository.kt: added RoutineExerciseDao constructor param; added WorkoutBootstrap data class; added instantiateWorkoutFromRoutine(routineId) that creates Workout, expands RoutineExercise into WorkoutSets, and returns ghostMap for previous-session hints. Compile: zero errors, KSP clean. DB version: 19→20.",
    "timestamp": "2026-02-22T12:00:00Z"
  }
  ,{
    "plan": "ToolsScreen (Clocks) — EMOM & Tabata Start button icon parity: added PlayArrow/Pause Icon + Spacer(8.dp) to the TABATA/EMOM branch Start button in ToolsScreen.kt to match the existing Stopwatch/Timer branch which already included icons. No layout or ViewModel changes; purely cosmetic parity fix. Files: ToolsScreen.kt, CLAUDE.md.",
    "timestamp": "2026-02-22T13:00:00Z"
  }
  ,{
    "plan": "ToolsScreen (Clocks) — QA fix: EMOM/Tabata buttons pushed off-screen by content overflow. Root cause: outer Column used fillMaxSize() with no vertical scroll, so the 2x2 mode grid + 72sp clock + multiple config text fields for EMOM/Tabata overflowed the viewport and the button Row was never rendered. Fix: added verticalScroll(rememberScrollState()) to Column modifier; added padding(top=16.dp) to EMOM/TABATA button Row. ViewModel resetTimer() confirmed correct (resets currentRound=0, phase=IDLE, displaySeconds=0, elapsedSeconds=0, isRunning=false). Files: ToolsScreen.kt.",
    "timestamp": "2026-02-22T14:00:00Z"
  }
  ,{
    "plan": "Precision Audio/Haptic Schema & EMOM Termination Fix (5 files): (1) AndroidManifest.xml — added VIBRATE permission after WAKE_LOCK. (2) WakeLockManager.kt — replaced SCREEN_BRIGHT_WAKE_LOCK|ACQUIRE_CAUSES_WAKEUP with PARTIAL_WAKE_LOCK (keeps CPU alive when screen off; removed @Suppress DEPRECATION). (3) RestTimerNotifier.kt — changed all ToneGenerator stream from STREAM_NOTIFICATION to STREAM_ALARM (bypasses DND/silent mode); added AlertType enum {ROUND_START, WARNING, COUNTDOWN_TICK, FINISH}; added private helpers playBeep(durationMs, volume), hapticShortPulse() [Pattern 1: 50ms pulse], hapticPhasePattern() [Pattern 2: 0/150/150/150/150/500ms waveform]; added public triggerAudioAlert(AlertType) API with synchronized audio+haptic per tier: ROUND_START=600ms+Pattern2, WARNING=2×150ms+Pattern1×2 (300ms apart), COUNTDOWN_TICK=200ms+Pattern1, FINISH=[150+150+800ms]+Pattern2; existing playTone/vibrate/playWarningBeep/notifyUser/notifyEnd kept for WorkoutViewModel compatibility. (4) ToolsViewModel.kt — added AlertType import; finishTimer() now calls triggerAudioAlert(FINISH) instead of notifyEnd(); runEmom() rewritten: removed skipLastRest/isLastRound block (was calling resetTimer() silently), now calls triggerAudioAlert(ROUND_START) at each round start, triggerAudioAlert(WARNING) at warn-at threshold, triggerAudioAlert(COUNTDOWN_TICK) at 2s/1s, always reaches finishTimer(); runTabata() rewritten: triggerAudioAlert(ROUND_START) at work+rest start, triggerAudioAlert(COUNTDOWN_TICK) at 2s/1s, triggerAudioAlert(FINISH) at work+rest phase end, finishTimer() at completion. (5) ToolsScreen.kt — removed 'Skip last rest period' Spacer+Row+Switch block from EMOM ConfigInputs branch (Tabata branch unchanged).",
    "timestamp": "2026-02-22T16:00:00Z"
  }
  ,{
    "plan": "PowerME Data Hardening Phase (DB v20→v21, 6 risks resolved, 14 files): RISK 3 — Workout.kt: routineId changed to nullable Long?, ForeignKey.CASCADE → SET_NULL; MIGRATION_20_21 recreates workouts table with nullable routineId, SET_NULL FK, isCompleted INTEGER NOT NULL DEFAULT 0; existing records migrated with isCompleted=1. RISK 5 — routine_exercise_cross_ref table dropped; stickyNote column merged into routine_exercises; RoutineExerciseCrossRef.kt + RoutineExerciseCrossRefDao.kt deleted; RoutineExercise.kt gains stickyNote field; RoutineExerciseDao gains getStickyNote+updateStickyNote; ActionExecutor.kt + WorkoutViewModel.kt migrate from CrossRefDao to RoutineExerciseDao. RISK 1 — WorkoutRepository.instantiateWorkoutFromRoutine() wrapped in database.withTransaction; PowerMeDatabase injected. RISK 2 — WorkoutSetDao.getPreviousSessionSets adds AND w.isCompleted = 1 filter. RISK 4 — WorkoutViewModel.finishWorkout() calls routineDao.updateLastPerformed(state.routineId, endTime); RoutineDao injected. RISK 6 (UX) — WorkoutDao adds WorkoutExerciseNameRow + getAllCompletedWorkoutsWithExerciseNames() Flow query (DISTINCT subquery + LEFT JOINs); WorkoutRepository delegates the flow; HistoryViewModel replaced getAllWorkouts with new query + collapseRows(groupBy id) → WorkoutWithExerciseSummary StateFlow; HistoryScreen WorkoutHistoryCard updated to accept WorkoutWithExerciseSummary and show exercise name chips row. DB_UPGRADE.md + CLAUDE.md + plans.json updated.",
    "timestamp": "2026-02-25T00:00:00Z"
  }
  ,{
    "plan": "PowerME Iron Vault Sprint (DB v21→v22, 3 features, 10 files): (1) Iron Vault DB Persistence — WorkoutSet.kt: isCompleted Boolean added; MIGRATION_21_22: ALTER TABLE workout_sets ADD COLUMN isCompleted INTEGER NOT NULL DEFAULT 0; WorkoutSetDao: updateWeightReps, updateSetCompleted, updateSetType, deleteIncompleteSetsByWorkout; WorkoutDao: getActiveWorkout() (isCompleted=0 LIMIT 1), deleteWorkoutById(); WorkoutRepository: WorkoutBootstrap gains workoutSets List<WorkoutSet> (DB-assigned IDs queried back after insertSets); createEmptyWorkout(), createWorkoutSet() added; WorkoutViewModel: workoutDao injected; rehydrateIfNeeded() on init restores active session from DB; startWorkoutFromRoutine() calls instantiateWorkoutFromRoutine and builds ExerciseWithSets with DB-assigned set IDs; startWorkout() async — creates DB record first; addExercise() and addSet() persist WorkoutSets to DB when workoutId non-null; debouncedSaveSet() (300ms) writes weight+reps on onWeightChanged/onRepsChanged for Valid inputs; completeSet() writes updateSetCompleted to DB immediately; finishWorkout() updates existing Workout row (no new insert) + deleteIncompleteSetsByWorkout; cancelWorkout() deletes sets + workout by ID from DB; availablePlates StateFlow from UserSettings. (2) SetType Tap-to-Cycle Badge — WorkoutSetRow: set-number Text replaced with clickable Box; (setLabel, setColor) mapped from SetType (NORMAL=setOrder/onSurface, WARMUP=W/MedicalAmber, FAILURE=F/StremioError, DROP=D/StremioMagenta); onCycleSetType lambda parameter added; ExerciseCard gains onCycleSetType param and wires to each WorkoutSetRow; ActiveWorkoutScreen items loop passes viewModel.cycleSetType; cycleSetType() in WorkoutViewModel cycles NORMAL→WARMUP→FAILURE→DROP→NORMAL and calls updateSetType DAO. (3) Per-Set PlateCalculatorSheet — old Calculate header button + inline plate calc Card removed from ExerciseCard; PlateCalculatorSheet ModalBottomSheet added (inputWeight var, WorkoutInputField, PlateCalculator.calculatePlates with UserSettings plates, error label); calc icon (20dp) inserted between weight+reps fields in WorkoutSetRow; ExerciseCard manages showPlateCalc+calcTargetWeight state; availablePlates: List<Double> parameter added to ExerciseCard; collected from workoutViewModel.availablePlates in ActiveWorkoutScreen. (4) Navigation — shared WorkoutViewModel at PowerMeApp level; WorkoutsScreen: onStartWorkout(Long) signature, Resume Workout banner when isWorkoutActive, RoutineCard Start button calling onStartWorkout(routine.id); PowerMeNavigation wires startWorkoutFromRoutine/startWorkout + passes shared VM to ActiveWorkoutScreen. Files: WorkoutSet.kt, PowerMeDatabase.kt, DatabaseModule.kt, WorkoutSetDao.kt, WorkoutDao.kt, WorkoutRepository.kt, WorkoutViewModel.kt, ActiveWorkoutScreen.kt, WorkoutsScreen.kt, PowerMeNavigation.kt, DB_UPGRADE.md, CLAUDE.md, plans.json.",
    "timestamp": "2026-02-25T12:00:00Z"
  }
  ,{
    "plan": "Strong-Inspired UI Overhaul (7 files): (1) WorkoutViewModel.kt — Added WorkoutSummary data class; ActiveWorkoutState gains workoutName, elapsedSeconds, pendingWorkoutSummary; elapsedTimerJob + startElapsedTimer() for per-second elapsed ticker; workoutName set to routine.name in startWorkoutFromRoutine/rehydrateIfNeeded, 'Empty Workout' in startWorkout; finishWorkout() signature changed (no onComplete param) — now sets pendingWorkoutSummary state instead of resetting; dismissWorkoutSummary() resets state; cancelWorkout() cancels elapsedTimerJob; onCleared cancels elapsedTimerJob. (2) WorkoutDao.kt — WorkoutExerciseNameRow gains routineName and setCount fields; getAllCompletedWorkoutsWithExerciseNames SQL updated with LEFT JOIN routines + COUNT subquery. (3) HistoryViewModel.kt — WorkoutWithExerciseSummary gains routineName and setCount; collapseRows() threads both fields from first row. (4) ActiveWorkoutScreen.kt — Added Close icon import; outer layout restructured: non-active state gets padding(16dp) on Box; active state gets compact Surface top bar (Close → workoutName+elapsedSeconds → Finish button) + RestTimerBar wrapped in padding Box + LazyColumn with padding(horizontal=16dp); removed ACTIVE WORKOUT header item and bottom Cancel/Finish buttons; cancel replaced by AlertDialog; post-workout driven by PostWorkoutSummarySheet composable (triggered by pendingWorkoutSummary != null); exercise name color primary→onSurface; muscle group color primary.copy(0.7f)→onSurface.copy(0.5f); STRENGTH column headers replaced (SET/PREVIOUS/KG/REPS/✓ layout); Add Set button replaced with minimal clickable Row; formatElapsed/formatDuration helpers added; PostWorkoutSummarySheet composable added. (5) WorkoutsScreen.kt — Added BorderStroke, background, AccessTime, IntrinsicSize.Min imports; 'Workouts' screen title added; Start Empty Workout changed to OutlinedButton; 'My Routines' → 'Routines'; RoutineCard restructured with left accent bar (3dp primary.copy(0.6f)), main Column, actions Column; SuggestionChip for exercise count; AccessTime icon for recency; FilledTonalButton for Start. (6) HistoryScreen.kt — Full rewrite: color tokens fixed (primary→onSurface/onBackground); WorkoutHistoryCard redesigned with title row (routineName fallback), HorizontalDivider, stats Row (Timer/FitnessCenter/List icons + duration/volume/setCount), exercise chips (FlowRow SuggestionChip, +N overflow); formatDuration helper; History header text.",
    "timestamp": "2026-03-07T12:00:00Z"
  },
  {
    "plan": "Strong-Inspired UI Overhaul — Test Suite: Created unit tests for business-logic changes. Files created/edited: (1) app/src/test/java/.../ui/history/HistoryViewModelTest.kt — 5 tests for collapseRows() via workouts StateFlow: single workout+exercise (all fields propagate), single workout+multiple exercises (exercise names collected), null routineName (no crash), multiple workouts (grouped+sorted desc by timestamp), null exerciseName (empty exerciseNames list). (2) app/src/test/java/.../ui/workout/WorkoutViewModelTest.kt — 6 tests using StandardTestDispatcher + runCurrent() pattern: 2a workoutName from routine, 2b workoutName='Empty Workout', 2c elapsed timer increments (advanceTimeBy(3_001) per exclusive-upper-bound semantics, cancel-before-assert pattern to prevent cleanup hang), 2d finishWorkout produces pendingWorkoutSummary, 2e dismissWorkoutSummary clears summary, 2f timer stops after cancelWorkout. (3) app/src/test/java/.../actions/ActionExecutorTest.kt — Fixed stale constructor (added 5 new mock params: ExerciseRepository, GoalDocumentManager, MedicalLedgerRepository, RoutineDao, RoutineExerciseDao). (4) app/src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker — Created mock-maker-inline extension to enable mocking of final Kotlin classes. Key fix: WorkoutViewModel.kt line 752 changed while(isActive) to while(true) to resolve deprecated FlowCollector.isActive ambiguity. All 21 tests green, no regressions.",
    "timestamp": "2026-03-08T08:20:00Z"
  },
  {
    "plan": "Comprehensive Test Coverage Audit — Added unit tests for three previously untested pure-logic utility/analytics classes. Files created: (1) app/src/test/java/.../util/SurgicalValidatorTest.kt — 18 tests covering parseDecimal (dot/comma separator, empty, blank, invalid chars, bare period/comma, negative), parseReps (integer, decimal rejected, empty, zero), and isLeakedMetric (null, empty, cm/kg/lbs/lb suffixes, long coaching cue passthrough, no-unit strings). (2) app/src/test/java/.../analytics/StatisticalEngineTest.kt — 22 tests covering mean (empty, 3-value), standardDeviation (single, sample formula), zScore (zero stdDev, normal), firstQuartile/thirdQuartile/IQR (empty, 8-value index behaviour), detectOutliersIQR (size<4, clear outlier), detectOutliersZScore (size<3, clear outlier), pearsonCorrelation (size mismatch, positive correlation delta), calculate1RM (zero reps, Epley formula), calculateBayesian1RM (zero sampleSize, zero priorMean, standard blend), rateOfChange (zero previous, 10% gain). (3) app/src/test/java/.../util/PlateCalculatorTest.kt — 13 tests covering calculatePlates (total<bar→null, bar-only, 2x20kg bench, fractional 22.5/side, unachievable with error>0.5, women's bar), parseAvailablePlates (standard string, empty, invalid entries skipped, duplicates deduplicated), formatPlateBreakdown (empty→'Bar only', 2x20+1x5, fractional 2.5kg). All 53 new tests pass; full suite (previous + new) green with no regressions.",
    "timestamp": "2026-03-08T10:00:00Z"
  },
  {
    "plan": "Strong-Inspired Layout & Sizing Alignment — Pure layout/typography changes across 4 files, no logic or color changes. WorkoutInputField.kt: height 34→40dp, corner radius 4→8dp, fontSize 14→15sp. ActiveWorkoutScreen.kt: STRENGTH column headers (SET 24→32dp, PREVIOUS 72→88dp, calc spacer 26→30dp, ✓ spacer 36→42dp, fontSize 10→11sp); WorkoutSetRow (row height 36→44dp, SET width 24→32dp, PREVIOUS width 72→88dp, plate calc icon box 20→24dp/icon 16→18dp, checkmark box 42dp/radius 8dp/fontSize 16→18sp, SET label fontSize 13→14sp); Add Set row (vertical padding 8→10dp, icon 16→18dp, fontSize 13→14sp). WorkoutsScreen.kt: screen title 24→28sp, Routines label 22→24sp, RoutineCard name 17→18sp/Bold→SemiBold, Start button 13→14sp, left accent bar 3→4dp. HistoryScreen.kt: screen title 24→28sp, card title 16→17sp, stat icons 14→16dp, stat text 13→14sp.",
    "timestamp": "2026-03-08T11:00:00Z"
  }
]
